package constants

// constants shared by multiple components to avoid magic numbers
// please, do no add a constant that is not shared by multiple components
var (
	Base10    = 10
	Base16    = 16
	BitSize64 = 64
)
