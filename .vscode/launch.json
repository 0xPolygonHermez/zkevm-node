{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Hermez node (Goerli)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "console": "integratedTerminal",
            "program": "${workspaceFolder}/cmd",
            "args": [
                "run", "--network", "internaltestnet", "--cfg", "${workspaceFolder}/config/config.itestnet.toml"
            ],
        }
    ]
} 